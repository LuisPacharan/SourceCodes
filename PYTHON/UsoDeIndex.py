#Buscar en qué indice se encuantra un elemeto.

lista = [8, 90, 1, 5, 44, 132, 600, 3, 4,1]

print(5 in lista)

index = lista.index(5)
print(index)

#Si en la lista existen tres elementos con valor a 5, se hará referencia al indice del primer elemento.

index = lista.index(1)
print(index)